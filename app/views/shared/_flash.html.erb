<! Use INSPECT to see the value of the variable, it comes in handy when you're debugging >
<% if flash[:notice] != nil %>
    <div class="alert alert-info mt-4 role" role="alert">
        <%= flash.now[:notice]%>
    </div>
<% end %>
<% if flash[:alert] %>
    <div class="alert alert-warning mt-4" role="alert">
        <%= flash.now[:alert]%>
    </div>
<% end %>